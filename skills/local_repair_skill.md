# Local Repair Skill

Use for a localized defect. Require a mask, keep context outside the mask fixed, and measure `preserve_change_rate`. Escalate to full regeneration only when the diagnosis says the failure is global or the local route fails after bounded retry.
