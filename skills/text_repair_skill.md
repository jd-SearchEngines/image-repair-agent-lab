# Text Repair Skill

Use when the defect is a label, word, number, or other rendered text. Preserve typography, baseline, font weight, container geometry, product silhouette, and all pixels outside the text mask. A real adapter must verify OCR before/after; the local simulator only verifies declared fixture pixels.
